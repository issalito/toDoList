import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#F7E6C4', // Set your desired primary color
        },
        background: {
            default: '#F7E6C4', // Set your desired background color
        },
    },
});

export default theme;